# Acceptance criteria — Tailor camp rooms and follow-up assignments to the active camp year: rooms page and room assignment scoped to active year by default with optional year selector; registration detail can assign rooms for that year only; year hub and follow-up link rooms/assignments for the selected/active year.

Generated by `tvs brief`. Observable checks for the drafted requirements.

## AC-1 — Intent delivered

Given a user opens the delivered experience,
when they complete the primary flow implied by "Tailor camp rooms and follow-up assignments to the active camp year: rooms page and room assignment scoped to active year by default with optional year selector; registration detail can assign rooms for that year only; year hub and follow-up link rooms/assignments for the selected/active year.",
then the flow succeeds without placeholder-only UI.

## AC-2 — Validation

Given invalid or missing required input,
when the user submits,
then the system rejects the action and explains what to fix.

## AC-3 — Happy path

Given valid input,
when the user completes the primary flow,
then the expected outcome of the intent is achieved.

## AC-4 — No secret leakage

Given the delivered source,
when inspected,
then no hard-coded passwords, API keys, or tokens are present.
