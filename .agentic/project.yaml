# TVS Agentic OS — project configuration
#
# Written by `tvs init`. Safe to edit.
#
# This file is SUBORDINATE to the constitution. It may make governance
# stricter for this project; it can never make it looser. Keys that would
# grant a command, waive an approval, skip a Definition-of-Done criterion
# or lower a risk level are refused by `tvs validate` rather than ignored.
#
# Schema: command_center/schemas/project.schema.json

project:
  id: campusgem-chms
  name: campusgem-chms
  type: web-app

tvs:
  version: 0.1.0
  constitution_version: 1.0.0
  initialized_at: "2026-09-18T15:47:37Z"

environments:
  - development
  - staging
  - production

workflow:
  default_type: FEATURE
  default_risk: MEDIUM

repository:
  provider: github
  default_branch: main
  url: git@github.com:isaackumi/portal-campusgem.git

technology:
  languages: [javascript, typescript]
  frameworks: [next]
  infrastructure: [vercel]

# Uncomment to make this project stricter than the defaults:
#
# governance:
#   minimum_risk: HIGH
#   require_human_approval_in: [staging, production]
#   additional_required_criteria: [load_test, accessibility_audit]

ai_integrations:
  - universal
  - codex
  - cursor
